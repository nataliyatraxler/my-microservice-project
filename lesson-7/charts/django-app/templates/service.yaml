apiVersion: v1
kind: Service
metadata:
  name: django-app
spec:
  type: {{ .Values.service.type }}
  selector:
    app: django-app
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
